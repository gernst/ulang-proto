define
  inf x = $ x :: inf x;
  test = inf 0;
  
  huge = 4 ^ 4;
    
  take (n +1) ($ x :: xs) = x :: take n xs;
  
  long = take huge test;
end

eval
  take 0 test;
  take 1 test;
  take 2 test;
  take 3 test;
  test;
end