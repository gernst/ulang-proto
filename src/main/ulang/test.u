definition
  f True  x y = x;
  f False x y = y;
  eq x x = True;
  eq x y = False;
  f a b c = (a,b,c);
  g = \a b c d e f -> \z -> (a,b,c,d,e,f, z);
  h = \a -> \a -> (a, b);
  f x A = x;
  f y B = y;
  split (f z) (x,y) = (f, z, x, y);
  (x, xs)
end

eval
  f True  A B;
  f False A B;
  
  eq A A;
  eq A B;
  
  f A B C;
  (g A B C D E F) Z;
  
  split (A B) (C, D);
end
